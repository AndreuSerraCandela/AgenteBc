[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "agente-bc"
dynamic = ["version"]
description = "Asistente de diagnóstico de errores de Business Central on-premises"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1,<4",
    "playwright>=1.55,<2",
    "python-dotenv>=1.0,<2",
    "requests>=2.32,<3",
]

[project.optional-dependencies]
sql = ["pyodbc>=5.2,<6"]
windows-auth = ["requests-negotiate-sspi>=0.5.2,<1"]
desktop = ["pywebview>=5,<6"]
build = ["pyinstaller>=6,<7"]
dev = ["pytest>=8.3,<9"]

[project.scripts]
agentebc = "agentebc.cli:main"
agentebc-web = "agentebc.webapp:main"
agentebc-desktop = "agentebc.desktop:main"
agentebc-portal = "agentebc.portal:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "agentebc.__version__"}

[tool.setuptools.package-data]
agentebc = ["templates/*.html"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
